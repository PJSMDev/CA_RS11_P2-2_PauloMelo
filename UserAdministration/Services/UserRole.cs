namespace UserAdministration.Services
{
    /// <summary>
    /// Enum representing different user roles.
    /// </summary>
    public enum UserRole
    {
        Admin,
        PowerUser,
        SimpleUser
    }
}
